#include "passenger.h"

int main() {
    Passenger_run("hello, continue there to la world.... fn while");

    /*
    HashMap map = HashMap_make();

    String names[] = {
        String_make("James"),
        String_make("Roisin"),
        String_make("Dylan"),
        String_make("Nathan"),
    };

    Value values[] = {
        Value_make_number(19),
        Value_make_bool(false),
        Value_make_number(642.0f),
        Value_make_string(String_make("What is up")),
    };
    
    for(i32 i = 0; i < 4; i++) {
        HashMap_set(&map, names[i], values[i]);
    }

    HashMap_free(&map);
    */
}
